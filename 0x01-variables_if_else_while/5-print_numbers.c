#include <stdio.h>
/**
 * main - return single digit
 * description: 'Return numbers from zero to nine
 * Return: always 0 (success)
 */
int main(void)
{
int num;
for (num = 0; num < 10; num++)
printf("%d", num);
printf("\n");
return (0);
}
